{{!-- Data is handed to this template via the homeroute --}}
{{#each blogs as |blog| }}
<div class="div-blog">
    <h1>{{blog.title}}</h1>
    <p>{{blog.content}}</p>
    <p>{{blog.user.name}}</p>
</div>
{{/each}}